import 'package:flutter/material.dart';

class DeleteHomePage extends StatelessWidget {
  const DeleteHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Delete'),
      ),
      body: Container(),
    );
  }
}
